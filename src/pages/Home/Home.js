import React from 'react';
import MainTemplate from 'components/templates/MainTemplate';

const Home = ({ handleChangeTheme }) => {
  return <MainTemplate handleChangeTheme={handleChangeTheme}></MainTemplate>;
};

export default Home;
